apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: interview-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/OWNER/REPO-frontend:latest
          ports:
            - containerPort: 80
