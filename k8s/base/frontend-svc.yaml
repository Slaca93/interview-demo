apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: interview-demo
spec:
  type: NodePort  # egyszerű demóhoz; ha van Ingressed, átállíthatod ClusterIP-re + Ingress
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
